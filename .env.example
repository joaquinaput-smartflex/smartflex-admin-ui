# Backend API URL (internal, never exposed to client)
BACKEND_URL=http://127.0.0.1:8000

# JWT Secret for signing session cookies (generate a strong random string)
JWT_SECRET=your-super-secret-key-change-this

# Session cookie name
SESSION_COOKIE_NAME=smartflex_session

# Node environment
NODE_ENV=production
